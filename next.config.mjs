/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.clerk.com'], // Erlaube das Laden von Bildern von img.clerk.com
    },
  };
  
  export default nextConfig;
  